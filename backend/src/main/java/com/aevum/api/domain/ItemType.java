package com.aevum.api.domain;

public enum ItemType {
    TEXT,   // Carta / Texto digitado localmente
    PHOTO,  // Imagem (JPGEG/PNG/HEIC)
    AUDIO,  // Gravação de som
    VIDEO   // Registro em vídeo (limitado)
}
